{
  "_comment": "Skips all relative/local paths (markdown-link-check treats them as URLs → false positives) plus common example/placeholder hosts in docs (localhost, your-org, etc.). cursor.com/automations links are auth-gated and bot-block anonymous CI requests (HTTP 403/0), so they are skipped too — the URLs stay visible in the runbooks for operators.",
  "ignorePatterns": [
    { "pattern": "^(?!https?://)" },
    { "pattern": "^https?://localhost" },
    { "pattern": "^https?://127\\.0\\.0\\.1" },
    { "pattern": "^https?://0\\.0\\.0\\.0" },
    { "pattern": "^https?://[^/]*example\\.(com|org|net)" },
    { "pattern": "^https?://cursor\\.com/automations" },
    { "pattern": "your-org" }
  ],
  "timeout": "20s",
  "retryOn429": true,
  "retryCount": 3,
  "fallbackRetryDelay": "30s",
  "aliveStatusCodes": [200, 206]
}
