# TG_parser dependencies (MVP)
# Based on docs/tech-stack.md

# Core
pydantic>=2.0,<3.0
pydantic-settings>=2.0
jsonschema>=4.0

# HTTP/Network
httpx>=0.27

# CLI
typer>=0.12

# HTTP API (v2.0)
fastapi>=0.115,<0.137
uvicorn[standard]>=0.32
slowapi>=0.1.9  # Rate limiting

# Database
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29  # PostgreSQL async driver
psycopg2-binary>=2.9  # PostgreSQL sync driver (for Alembic migrations)
alembic>=1.13
pgvector>=0.3.0  # Vector similarity search (RAG)

# Telegram
telethon>=1.36

# Logging
structlog>=24.0

# Agents SDK (PoC - Session 14)
openai-agents>=0.6

# Configuration
PyYAML>=6.0
python-dotenv>=1.0  # Load .env for SDKs that need os.environ

# MCP Server
mcp>=1.25

# Telegram Bot
aiogram>=3.15

# F11 watchlist keyword normalization (script-routed lemmatization)
pymorphy3>=2.0
pymorphy3-dicts-ru>=2.4
simplemma>=1.0

# Observability
prometheus-fastapi-instrumentator>=7.0
apscheduler>=3.10

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.0
# Ephemeral pgvector containers for migration smoke / parity tests (DI-9 phase 2)
# — opt-in via TEST_TESTCONTAINERS=1; requires a Docker daemon on the host.
testcontainers[postgres]>=4.8

# Code quality
ruff==0.15.11
