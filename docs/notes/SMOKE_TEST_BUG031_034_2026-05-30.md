# PRODUCTION REAL-FIRE SMOKE TEST — `subscribe_digest` / ConfirmFlow cluster (BUG-031 / 032 / 033 / 034)

**Prepared:** 2026-05-30. **Repo:** `/Users/alexanderefimov/TG_parser`. **Branch:** `main`.
**Run by:** a **human operator** in a **real Telegram group** (MTProto user — the bot ignores
bot-API peers and basic-group privacy mode, so this cannot be automated; a real person must type).
**Why a human runs it:** these are NL → FSM → executor traces that only reproduce against the live
Gemini-backed bot in a real group; no harness substitutes for a real Telegram user account.

This runbook **verifies behavior**, not deployment. Deploy is already confirmed fresh on `39b6ba2`
and the running container provably contains all four fixes
(`subscribe_target_chat_id_overridden`, `_resolve_target_for_bot_subscribe`,
`validate_channel_username`). The single deploy checkbox below is a **re-confirm**, not a deploy step.

> **Closure precedent:** mirror how BUG-037 was closed (`docs/notes/HANDOFF_BUG037_2026-05-30.md`):
> the fix is "real-fire verified" only when the original traces are re-run against live prod and the
> correct behavior is observed + evidenced (screenshots / copied bot text). Until then the
> `BUG_LOG.md` entries stay flagged "prod real-fire smoke pending".

---

## 0. Preconditions (re-confirm before testing)

- [ ] **Bot identity:** target bot is `@Tgingest_bot` (id `8657845219`).
- [ ] **Test group:** `vps-watch-test-grp`, real `chat_id = -5279672667`.
- [ ] **Bot is admin in the group** (basic-group privacy mode is ON by default → the bot will NOT
      see free-text NL prompts unless promoted to admin; this was the Test-D blocker on 2026-05-24).
- [ ] **You are testing as a real Telegram user** (MTProto), not via bot API. (This is why a human runs it.)
- [ ] **Deployed SHA re-confirm** — run and verify the output is `39b6ba2` (or a later descendant):

```bash
ssh prod 'cd ~/TG_parser && git log --oneline -1'
```

- [ ] Output line begins with `39b6ba2` **or newer** (≥ `39b6ba2`). If it shows anything OLDER → **STOP**:
      you would be testing stale code; do not run the cases below, escalate the deploy first.
- [ ] **Original leak reminder (context):** the BUG-033 leak was the bot emitting placeholder
      `chat_id=123` instead of the real group `-5279672667`. The smoking-gun channel typo was
      «pro fendocrinologist» (with a space) silently becoming `pro_fendocrinologist`; the real channel
      is `profendocrinologist`.
- [ ] **Have a cleanup path ready** (see § 5) — do NOT leave a live hourly digest in the test group.

---

## 1. BUG-033 (Critical) — real group `chat_id`, never `123`

**Goal:** an «этот чат» NL intent from inside the group must bind the subscription/preview to the
REAL group `chat_id = -5279672667`, never the hallucinated placeholder `123`.

**Send (in the group, as a real user):**

```text
Подпиши этот чат на ежечасный дайджест канала profendocrinologist
```

**Expected correct behavior:**
- Bot replies with a **preview / «Подтвердите …»** prompt (see BUG-031) whose target is the **current
  group**, NOT chat `123`.
- After you confirm (§ 3 / § 4), the bound target is the real group `-5279672667`.

**Confirm the bound `chat_id`** (any ONE is sufficient evidence):
- [ ] The preview / confirmation text references the current group (or shows the real `chat_id`/group
      name), with **no** `123` anywhere.
- [ ] `list_digests` (MCP, or the bot's list-subscriptions equivalent) shows the new subscription bound
      to `-5279672667`.
- [ ] A digest actually **arrives in this group** on a cron tick (most decisive — see § 4 optional step).

**PASS / FAIL:**
- [ ] **PASS** — subscription/preview is bound to / digest is delivered to the real group `-5279672667`.
- [ ] **FAIL** — any sign of `chat_id=123`, an undeliverable target, or a digest that never arrives /
      targets a foreign chat.

---

## 2. BUG-031 (Severe) — preview FIRST, no DB row before confirm

**Goal:** the bot must show a **preview / «Подтвердите, пожалуйста, … [да/нет]»** BEFORE creating
anything. No «Подписка создана» and no DB row may appear before you confirm.

**Send (in the group):**

```text
Подпиши этот чат на ежечасный дайджест канала profendocrinologist
```

**Expected correct behavior (ordering is the assertion):**
1. Bot emits a **preview** / «Подтвердите …» prompt.
2. **Nothing is persisted yet** — no «📰 Подписка создана» message, and `list_digests` shows **no new row**.
3. Only after your affirmative (§ 3) does the bot create the subscription and confirm it.

**Confirm no premature write** (before sending «да»):
- [ ] You see the preview / «Подтвердите …» message.
- [ ] You do NOT see «📰 Подписка создана» yet.
- [ ] (Optional, decisive) `list_digests` at this moment shows **no** new subscription.

**PASS / FAIL:**
- [ ] **PASS** — preview precedes any creation; no row exists until confirmation.
- [ ] **FAIL** — «📰 Подписка создана» (or a `list_digests` row) appears BEFORE you confirm
      (the original inverted-contract trace).

---

## 3. BUG-032 (Medium) — affirmative tokens accepted; garbage keeps FSM armed

**Goal:** at the confirm step, «да», «подтверждаю», «yes», «ok» must EACH be accepted as affirmative;
a clearly-unrecognized reply must keep the FSM armed and surface a helpful «accepted tokens» prompt
(NOT the old opaque «Я не совсем понимаю ваш ответ»).

> Run this across a couple of fresh preview attempts (re-issue the § 2 message each time to re-arm the
> preview). You do not need to actually create 4 subscriptions — cancel/clean up between affirmative
> checks, or only let the final one go through.

**3a — «да»**
- Arm a preview (send the § 2 message), then reply:

```text
да
```

- [ ] **PASS** — affirmative accepted, flow proceeds to creation.   [ ] **FAIL** — «да» rejected / «Я не совсем понимаю ваш ответ».

**3b — «подтверждаю»**
- Arm a fresh preview, then reply:

```text
подтверждаю
```

- [ ] **PASS** — accepted.   [ ] **FAIL** — rejected.

**3c — «yes»**
- Arm a fresh preview, then reply:

```text
yes
```

- [ ] **PASS** — accepted.   [ ] **FAIL** — rejected.

**3d — «ok»**
- Arm a fresh preview, then reply:

```text
ok
```

- [ ] **PASS** — accepted.   [ ] **FAIL** — rejected.

**3e — garbage / unrecognized reply (FSM must stay armed with a helpful prompt)**
- Arm a fresh preview, then reply with something clearly not yes/no:

```text
ммм не знаю
```

- [ ] **PASS** — bot keeps the FSM armed and replies with a **structured «accepted tokens» hint**
      (e.g. «Ответьте «да» / «подтверждаю» или «нет» / «отмена»»); a subsequent «да» still works.
- [ ] **FAIL** — bot emits the old opaque «Я не совсем понимаю ваш ответ» and/or drops the FSM so
      «да» no longer confirms.

**Overall BUG-032:**
- [ ] **PASS** — all of «да» / «подтверждаю» / «yes» / «ok» accepted; garbage handled gracefully.
- [ ] **FAIL** — any affirmative rejected, or opaque catch-all on garbage.

---

## 4. BUG-034 (Medium) — embedded-space channel name rejected / clarified

**Goal:** a channel name with an embedded space («pro fendocrinologist») must make the bot
**ASK TO CLARIFY / REJECT** (ideally suggesting `profendocrinologist`), NOT silently store
`pro_fendocrinologist` (space → underscore) and create an undeliverable subscription.

**Send (in the group):**

```text
Подпиши этот чат на ежечасный дайджест канала pro fendocrinologist
```

**Expected correct behavior:**
- Bot rejects / asks to clarify the channel name (e.g. «Похоже, имя канала содержит пробел — вы имели
  в виду `profendocrinologist`?»), citing the invalid-username reason.
- No subscription is silently created with `channel_ids=["pro_fendocrinologist"]`.

**Confirm:**
- [ ] Bot's reply flags the space / invalid username and/or suggests `profendocrinologist`.
- [ ] (Optional, decisive) `list_digests` shows **no** subscription with `pro_fendocrinologist`.

**PASS / FAIL:**
- [ ] **PASS** — bot rejects or asks to clarify the typo.
- [ ] **FAIL** — bot silently underscores to `pro_fendocrinologist` and/or creates an undeliverable
      subscription with that wrong channel id.

---

## 5. End-to-end happy path + cleanup

**Happy path (one full correct flow):**

1. Send (in the group):

```text
Подпиши этот чат на ежечасный дайджест канала profendocrinologist
```

- [ ] **Preview appears** (BUG-031) — and it is bound to the real group, not `123` (BUG-033).
2. Confirm:

```text
да
```

- [ ] **Subscription created** and bound to `chat_id = -5279672667` (verify via the confirmation text
      and/or `list_digests`).
- [ ] (Optional, most decisive for BUG-033) Let one cron tick fire (hourly, at `0 * * * *`) and confirm
      a **digest actually arrives in this group** for `profendocrinologist`.

**Cleanup (MANDATORY — do not leave a live hourly digest in the test group):**

3. List subscriptions and capture the new subscription UUID:
- [ ] `list_digests` (MCP from Cursor, or the bot's list equivalent) → note the new `subscription_id`.
4. Unsubscribe it:
- [ ] `unsubscribe_digest(subscription_id=<uuid>)` (MCP), or the bot equivalent.
- [ ] Re-run `list_digests` → confirm the test subscription is **gone** and the group has **no** live
      hourly digest left.
5. Also remove any stray rows accidentally created during § 3 affirmative-token attempts (3a–3d), if any.

---

## 6. Evidence to capture (for closure — mirror BUG-037 closure style)

Capture and attach to the closure note (screenshots or copied text):
- [ ] **Deploy re-confirm:** the `git log --oneline -1` line showing ≥ `39b6ba2`.
- [ ] **BUG-033:** bot reply / `list_digests` row showing bound `chat_id = -5279672667` (and the absence
      of `123`); ideally a screenshot of a digest delivered **into the group**.
- [ ] **BUG-031:** screenshot of the **preview message preceding** any «Подписка создана» (ordering),
      plus a `list_digests` snapshot showing no row before confirmation.
- [ ] **BUG-032:** screenshots of «да» / «подтверждаю» / «yes» / «ok» each being accepted, and of the
      structured «accepted tokens» hint on the garbage reply.
- [ ] **BUG-034:** screenshot of the bot rejecting / clarifying «pro fendocrinologist» (suggesting
      `profendocrinologist`), plus `list_digests` showing no `pro_fendocrinologist` row.
- [ ] **Cleanup:** `list_digests` before/after `unsubscribe_digest` proving the test digest was removed.
- [ ] **Timestamps** of each bot reply (for cross-referencing with prod logs if needed).

---

## 7. Interpreting results

- **Deploy is already confirmed fresh on `39b6ba2`** (running container provably contains all four
  fixes). So unlike the start-prompt's deploy-gated caveat, you do **NOT** need to suspect stale deploy
  here — the § 0 checkbox already rules it out.
- **If every case PASSES** → the BUG-031 / 032 / 033 / 034 cluster is **real-fire verified**. Record
  closure: the operator updates the `BUG_LOG.md` entries (drop the "prod real-fire smoke pending" flag,
  add the verified SHA `39b6ba2` + which traces were confirmed closed), mirroring the BUG-037 closure.
- **If any case FAILS** → because the deploy is confirmed fresh, this is **NOT** a stale-deploy artifact —
  it is a **genuine residual gap**. Do **NOT** silently re-patch a "closed" bug. Open a **NEW `BUG-NNN`**
  entry in `docs/notes/BUG_LOG.md` capturing: the exact message sent, the bot's actual reply, the
  expected behavior, the confirmed SHA `39b6ba2`, and the evidence; fix on a fresh `fix/...` branch.

---

## Quick reference

| Item | Value |
|---|---|
| Bot | `@Tgingest_bot` (id `8657845219`) |
| Test group | `vps-watch-test-grp`, `chat_id = -5279672667` |
| Confirmed deployed SHA | `39b6ba2` (re-confirm via `ssh prod 'cd ~/TG_parser && git log --oneline -1'`) |
| Real channel | `profendocrinologist` |
| Typo (must be rejected/clarified) | «pro fendocrinologist» (space) → wrong `pro_fendocrinologist` |
| Placeholder leak (must NOT recur) | `chat_id=123` |
| Fix commits (in running container) | BUG-033 `e50449b` · BUG-034 `6ebad33` · BUG-031+032 `66e8297` |
| Specs | `docs/notes/BUG_LOG.md` § BUG-031 / 032 / 033 / 034 |
| Closure precedent | `docs/notes/HANDOFF_BUG037_2026-05-30.md` |

---

## Results 2026-05-31 (real-fire run — human operator, group `vps-watch-test-grp`, prod SHA `39b6ba2`)

**Verdicts on the original cluster:**

- **BUG-031 (preview-before-write): PASS.** Every subscribe attempt showed `Preview … Подтвердите [да/нет]` FIRST; «📰 Подписка создана» only ever appeared AFTER an affirmative. Verified ~5×.
- **BUG-032 (affirmative tokens at the subscribe-confirm gate): PASS.** «да», «yes», «подтверждаю», «ок» all accepted; «нет» / «no» correctly cancelled.
- **BUG-034 (embedded-space channel name): PASS (core).** «pro fendocrinologist» → explicit rejection «Канал «pro fendocrinologist» содержит пробелы … Возможно, вы имели в виду «profendocrinologist»?», never silently underscored.
- **BUG-033 (real chat_id, not 123):** confirmed separately by the parent (live `list_digests` chat_id check) — not re-adjudicated here.

Real-fire closure lines added to `BUG_LOG.md` § BUG-031 / 032 / 034 (Status unchanged — `resolved`), mirroring the BUG-037 closure precedent.

**FOUR NEW residual gaps surfaced in the conversational / clarification layer** (all `Status: open`, filed 2026-05-31 in `BUG_LOG.md`):

| New bug | Severity | One-line gap | Root cause (file:line) |
|---|---|---|---|
| **BUG-039** | Severe | Channel-name clarification suggestion is a dead-end; «да» on it falls through to the opaque «Я не совсем понимаю ваш ответ» | Clarification is a tool **error**, not a `{"preview": True}`, so no `ConfirmFlow` armed (`channel_id.py:181-195` → `tools.py:2556-2558` → `agent.py:306-319` → `handlers.py:391-392`); bare «да» hits stateless LLM (`agent.py:181-183`) |
| **BUG-040** | Severe | Bare channel-name reply mid-flow routed to WRONG intent, non-deterministically (update_channel vs list_topics) | `process_message` is stateless across messages — no conversation history (`agent.py:181-183`); LLM `mode="AUTO"` guesses from a context-free token (`agent.py:398-400`) |
| **BUG-041** | Medium | Deterministic BUG-034 space-guard bypassable — Gemini sometimes strips the space upstream of the validator | LLM-side normalisation before the tool call; validator only sees `functionCall.args` (`agent.py:283, 296-303` → `tools.py:2554-2560`) |
| **BUG-042** | Minor | Preview cron truncated to «0» (LLM-paraphrased preview) while creation confirmation shows full «0 * * * *» | Preview text is LLM-re-authored (`tools.py:2635-2639` `message` → `functionResponse` `agent.py:330-337` → LLM text `agent.py:267-275`); creation msg is deterministic code (`tools.py:2704-2713`) |

**Common thread:** BUG-039 and BUG-040 share a systemic root — the agent has no general conversational memory; cross-turn continuity exists only via the explicit FSM (`ConfirmFlow`/`PaginationFlow`) and read-side `read_context`, neither of which covers a mid-subscribe clarification. BUG-041 and BUG-042 both stem from LLM-mediated surfaces (channel-name extraction, preview rendering) sitting in front of otherwise-deterministic code. Fixes are a SEPARATE future task.
