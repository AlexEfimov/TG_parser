# Handoff — TG_parser, продолжение после BUG-084 + follow-ups (2026-07-16)

## Состояние
- `main` = prod = `33d512a`. Рантайм-дрейфа нет (последний деплой — PR #318, envelope на MCP search).
- OpenAI embedding-квота восстановлена; RAG semantic/hybrid работает.

## Что сделано (смёржено в `main` + задеплоено)
- **BUG-084 (#311):** классификация `429` по `error.code` (terminal `insufficient_quota` → `EmbeddingQuotaError` без retry; transient → jittered backoff → `EmbeddingRateLimitError`); RAG semantic/hybrid→keyword fallback + `degraded` (HTTP `/search`,`/ask`; MCP `ask_question`); метрика `tg_embedding_requests_total{outcome,stage}`. Quality-gate: self-review + Bugbot clean.
- **ЗК-гейт (#312):** добавлен `ruff format --check`.
- **Алерты (#313):** `EmbeddingQuotaExhausted` + `EmbeddingRateLimitedSustained` (promtool-проверены).
- **BUG-084 resolved (#314);** регресс-тест BUG-081 (#317); BUG_LOG hygiene 8 багов→`resolved` (#316); Phase-1 t0 baselines (#315).
- **Phase 2 (#318):** MCP `search_knowledge_base` теперь `SearchResults{result, degraded}` (structured-output additive; меняется только unstructured `content`). Doc close-out + link-checker ignore для `cursor.com` (#319). Задеплоено, smoke green.
- **Phase-1 t1 re-snapshot (#320):** W1 BUG-084 soak PASS, W3 S5/S6 PASS, W2 S3 dedup INTERIM (рано).

## Открытые / запланированные
1. **НАБЛЮДЕНИЕ:** создана Cursor Automation (daily 13:00 UTC, локальный запуск, docs-PR) для авто-пересъёма t2. Целевое: S3 billing-clean дочит 48–72ч (07-17 ~48ч, 07-18 ~72ч) — финальный вердикт W2; + подтверждающий W1/W3. **ВАЖНО:** отключить автоматизацию вручную после финального вердикта W2. Бейзлайн/критерии: `docs/notes/PHASE1_WATCH_BASELINE_2026-07-15.md`.
2. **Фаза 3 (не начата):** F9 Security Hardening (нужно планирование/START_PROMPT), Configurable Embedding Provider (gated — поднимать только при повторе `quota_exhausted`). См. `FUTURE_FEATURES.md`.
3. **Мелочь:** BUG-008 monitor-only.

## Референсы
- `docs/notes/BUG_LOG.md` §BUG-084; `docs/notes/FUTURE_FEATURES.md`; `docs/notes/PHASE1_WATCH_BASELINE_2026-07-15.md`; `docs/runbooks/S1_S3_DEPLOY_AND_WATCH.md`; `docs/notes/WORKFLOW_REMEDIATION_SESSIONS_AGREEMENTS_2026-07-07.md` (ЗК §5).
- Prod: `ssh prod` (212.72.189.15:2296), `/home/user/TG_parser`, docker compose + Prometheus/Grafana.
- Конвенции: коммит только по явному запросу; PR-мёрж merge-commit + `--delete-branch`; ЗК = `ruff check` + `ruff format --check` + pytest (default & `TEST_POSTGRES=1`).
